const express = require("express");
const router = express.Router();

router.route("/createAds").post;
router.route("/editAd/:id").post;
router.route("/changeActivation/:id").post;

module.exports = router;
