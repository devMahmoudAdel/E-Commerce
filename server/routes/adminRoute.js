const express = require("express");
const router = express.Router();

router.route("createAdmin").post;
router.route("edit/:id").post;
router.route("getAll").get;
router.route("delete/:id").post;

module.exports = router;
